plugin_cache_dir  = "$HOME/.terraform.d/terraform-plugin-cache" 
disable_checkpoint = true

provider_installation {
  filesystem_mirror {
    path    = "/opt/terraform-mirrors"
    include = ["registry.terraform.io/*/*"]
  }
}